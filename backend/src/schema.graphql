# import * from './generated/prisma.graphql';
type Mutation {
  createItem(title: String!, description: String!, price: Int!, 
  image: String, largeImage: String, frets: String, wood: String): Item!
}

type Query {
 items: [Item]!
}